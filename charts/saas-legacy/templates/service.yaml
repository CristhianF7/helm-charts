apiVersion: v1
kind: Service
metadata:
  name: {{ include "saas-legacy.fullname" . }}
  labels:
    {{- include "saas-legacy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "saas-legacy.selectorLabels" . | nindent 4 }}
